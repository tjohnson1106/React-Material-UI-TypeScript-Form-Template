import React from "react";

import { MainForm } from "./MainForm";

const App = () => {
  return (
    <div
      style={{
        textAlign: "center"
      }}
    >
      <MainForm onSubmit={() => {}} />
    </div>
  );
};

export default App;
